# Shooting-place-vr

Краткое описание проекта в одно-два предложения. Например: "VR-тир для демонстрации механик стрельбы и взаимодействия с объектами".

---

## 🛠️ Окружение для разработки

Для работы с проектом необходимо следующее программное обеспечение:

*   **Unity Editor Version:** `6000.0.62f1` (Unity 6)
*   **IDE:** `JetBrains Rider` или `Visual Studio` с установленным пакетом "Разработка игр с помощью Unity".

---

## 🚀 Процесс работы с Git (Git Flow)

Мы используем Git Flow, где основной веткой для разработки является `develop`, а `master` (`main`) используется для стабильных, протестированных версий. Весь процесс построен на работе с ветками и Pull Request'ами.

**Правила работы:**

1.  **Одна задача — одна ветка.** Никогда не работайте напрямую в ветках `develop` или `master`. Для каждой новой фичи или исправления бага создавайте новую ветку от **актуальной** версии `develop`.

2.  **Наименование веток.** Используйте префиксы для понимания сути ветки:
    *   `feature/task-name` (например, `feature/pistol-shot-animation`) — для новой функциональности.
    *   `fix/bug-description` (например, `fix/gun-model-disappears`) — для исправления ошибок.

3.  **Создание Pull Request (PR).** После того как вы закончили работу в своей ветке и отправили коммиты в удаленный репозиторий (`git push`), создайте **Pull Request** из вашей ветки в ветку `develop`.
    *   В описании PR кратко опишите, что было сделано.

4.  **Code Review и Тестирование.** Ваш PR должен быть проверен как минимум одним другим участником команды (code review).
    *   **Самое важное:** Перед тем как слить ветку в `develop`, автор ветки и/или тестировщик **обязаны** протестировать новую функциональность в Unity, чтобы убедиться, что она работает корректно и не ломает ничего в проекте.

5.  **Слияние (Merge).** Только после успешного code review и тестирования Pull Request может быть слит (merged) в ветку `develop`.

Этот процесс обеспечивает стабильность основной ветки разработки и позволяет отслеживать все изменения в проекте.
